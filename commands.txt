ngrok start --config=./ngrok.yml --all  [to start the node js servers in the backend]
gsutil cors set cors.json gs://elixpoai.appspot.com [to set no cors on te firebase storage]
3c4053d7847629d1d2210b93a5ae219551bc430f [access token for github commit/pull/push req]
PAT == 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
ngrok start --config=./ngrok.yml --all [command run in the ~ directory to run all servers]
sudo nano ngrok.yml [command run in the ~ directory to edit the server directories!]


Added a branch on [30-07-2024]
PUSHING TO BRANCH


script used to update the elixpo links --> [NODE UPDATE_SEREVR.JS (elixpo.ai/)]
script to get internet connection UP --> [PYTHON WEBCONNECT.PY (/home/pi)] added to cornjob

bash script to get the link upgrade script and starting localhost [elixpo_ai_starter.sh (/home/pi)] added to system startup
systemd file [start_elixpo_ai_server.service (sudo nano /etc/systemd/system/)] running as of system startup!

first visit /etc/systemd/system
then execute sudo systemctl status start_elixpo_ai_server.service
or sudo systemctl enable start_elixpo_ai_server.service
or sudo systemctl start start_elixpo_ai_server.service

or edit using sudo nano start_elixpo_ai_server.service

server_update.js (updates both the link [using server_update.js] and starts the localhosts [using server.sh])
elixpo_ai_starter.sh starts the  server_update.js
systemd file start_elixpo_ai_server starts the elixpo_ai_starter.sh file on startup!

github file access token --> 3c4053d7847629d1d2210b93a5ae219551bc430f


updated port 3000 to handle the image fetching and ping
updated port 3001 to handle the tags python code 
updated port 3002 to handle the pimp prompt section 


systemd command to start the server shell for elixpo_ai (server_update.js) ==>

[Unit]
Description=Run Node.js script when internet is connected
After=network-online.target
Wants=network-online.target

[Service]
ExecStart=/home/pi/elixpo_ai_starter.sh
Restart=always
RestartSec=8
User=pi

[Install]
WantedBy=multi-user.target

new systemd name on RPI4 -- server_starter_elixpo_ai.service
sudo systemctl enable server_starter_elixpo_ai.service
sudo systemctl start server_starter_elixpo_ai.service
sudo systemctl status server_starter_elixpo_ai.service


wb api key - be4164e2510a26a0499aa79fa88c5bb3c43a3731

for vercell deployment

1. no token use and display -- moving the wifi symboll closer [ checked]
2. no instagram upload [checked]
3. no metamask [checked]
4. make the gallery button hidden (like coming soon) [checked]
5. make sure no one can F12 
6. rather than sending requests to the enhance-prompt just make enhance = true that's it [checked]
7. don't provide any watermarks [checked]
8. removed tags generator [checked]
9. added way to reach back to the docs [checked]


1. change the lazy loading effect to have cached memory from the server
2. allow changes to be pulled from the user-client side
3. add button for more loading and a progress bar for details of the progress 
